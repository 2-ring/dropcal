{
  "name": "dropcal-extension",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs && npm run copy",
    "copy": "mkdir -p dist/popup dist/sidebar dist/icons dist/fonts && cp manifest.json dist/ && cp -r icons/* dist/icons/ && cp -r fonts/* dist/fonts/ && cp popup/popup.html dist/popup/ && cp popup/popup.css dist/popup/ && cp sidebar/sidebar.html dist/sidebar/ && cp sidebar/sidebar.css dist/sidebar/",
    "watch": "node build.mjs --watch",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "esbuild": "^0.24.2",
    "typescript": "~5.7.0"
  },
  "dependencies": {
    "@phosphor-icons/web": "^2.1.2"
  }
}

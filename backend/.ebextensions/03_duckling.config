# Duckling Docker setup moved to .platform/hooks/prebuild/02_duckling.sh
# which has a 3600s timeout (vs ~600s for ebextensions commands).
# The DUCKLING_URL env var is set in 02_pip_no_cache.config.

client_max_body_size 25M;

# SSE streaming support — disable buffering for event-stream responses
# so Nginx forwards chunks immediately instead of accumulating them
map $http_accept $proxy_buffering_flag {
    ~text/event-stream off;
    default on;
}

proxy_buffering $proxy_buffering_flag;
